#include <stdio.h>

#include <stdio.h>
void reverse(int n)
{
    int d,r;
    while(n>0)
    {
        d=n%10;
        n=n/10;
        r=r*10+d;
        
    }
    printf("%d",r);
}

int main()
{
    int n;
    scanf("%d",&n);
    reverse(n);
    return 0;
}
