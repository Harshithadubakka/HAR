#include <stdio.h>

int main()
{
    int a;
    scanf("%d",&a);
    if((a%4==0)&&(a%100!=0 || a%400==0)){
    printf("%d the given year is leap year",a);
    }
    else{
    printf("%d the given  year is not leap year",a);
    }

    return 0;
}
